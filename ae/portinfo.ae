// Build/link smoke symbol for the Aether source set. Kept as a tiny exported
// function so the mixed C/Aether link path has an explicit, stable probe.

export aether_linked

aether_linked() -> int {
    return 1
}
